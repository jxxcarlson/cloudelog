I want to change the nature of the logs so that a log entry has not just
one quantity, description pair, but a list thereof.  Moreover, we need to 
change the nature the log itself, so that it has a list of units, where
the first unit goes with the first quantity, description pair, the second with the second, etc.  Most logs will have just one (quantity, descrition, unigs), but there are important exceptions.

